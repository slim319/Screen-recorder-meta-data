# Screen Recording Metadata Extractor

A Python script that extracts metadata (container, video, and audio track info) from a media file and prints it to the console.

## Usage

```bash
python src/extract_metadata.py <path-to-video.mp4>
